apply plugin: 'com.android.library'
apply from: '../config.gradle'

android {

    defaultConfig {
        // 混淆配置
        consumerProguardFiles 'proguard-base.pro'
    }
    // 数据绑定
    dataBinding {
        enabled = true
    }
}